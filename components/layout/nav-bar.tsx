import { NavBarClient } from "@/components/layout/nav-bar-client";

/** Server shell for the site nav — interactive behavior lives in `NavBarClient`. */
export function NavBar() {
  return <NavBarClient />;
}
