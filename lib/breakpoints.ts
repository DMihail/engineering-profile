/** Canonical layout breakpoints — keep CSS `width` queries in sync. */
export const BP_LG_PX = 1024;

/** Modern media query (CSS `width` range syntax). */
export const MQ_LG_UP = `(width >= ${BP_LG_PX}px)`;
